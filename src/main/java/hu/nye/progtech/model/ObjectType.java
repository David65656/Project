package hu.nye.progtech.model;

/**
 * The `ObjectType` enum represents the types of objects that can exist in a game environment.
 * <p>
 * It defines constants for common types such as WALL, PIT, WUMPUS, HERO, and GOLD.
 */

public enum ObjectType {
    WALL, PIT, WUMPUS, HERO, GOLD;
}
