package hu.nye.progtech.model;

/**
 * The `Direction` enum represents the cardinal directions in which a hero can view: North, South, West, and East.
 * <p>
 * This enum is used to indicate the direction a hero is facing or moving in a game environment.
 */

public enum Direction {
    North, South, West, East;
}
